# todo: 시스템 트레이 (F4)

## 작업 목표
앱을 Dock에 표시하지 않고 macOS 상단 메뉴바에만 상주시킨다. 트레이 아이콘 클릭 시 드롭다운 메뉴를 제공한다.

## 세부 작업
- [ ] `app/ui/tray.go`: `getlantern/systray`로 트레이 아이콘 초기화
- [ ] 트레이 메뉴 항목 구성
  - 캡처 시작
  - 스크롤 캡처 시작
  - 설정 열기 (브라우저로 `localhost` 오픈)
  - ☕ 개발자 응원하기 (후원 페이지 링크)
  - 앱 종료
- [ ] Dock 미표시 설정 (`Info.plist` 또는 런타임 설정)
- [ ] 앱 종료 시 정리 로직 (서버 종료, 단축키 해제)

## 참고
- `systray.Run()`은 macOS 요구사항으로 메인 스레드를 점유한다.
- 웹서버와 단축키 리스너는 별도 고루틴으로 실행한다.
